<%= form_with(model: meal, local: true) do |form| %>
  <!-- Ваш код формы для редактирования блюда -->
  <%= form.label :name %>
  <%= form.text_field :name %>

 
  <%= form.label :date %>
  <%= form.date_field :date %>

  <%= form.submit %>
<% end %>